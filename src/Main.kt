//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
fun main() {
    val x  = "hello"
    val builder = StringBuilder("Hello")

    println(x.contentEquals(builder))// here we are compare specific string with another string using builder method which are compare to x's string
    }
